/* eslint-disable react/prop-types */
import { useState, useEffect, Fragment } from 'react'
import { auth } from '../firebase/firebase'

const SendToCalendar = ({ onSubmit }) => {
  /*
   * Referencia:
   * https://github.com/InteractionDesignFoundation/add-event-to-calendar-docs/blob/main/services/google.md
   */
  const eventData = props.eventData
  const BASEURL = 'https://calendar.google.com/calendar/event?action=TEMPLATE'
  const AMP = '&amp;'
  let TEXT = 'Titulo del evento'
  TEXT = TEXT.replace(/ /g, '+')
  const DATES = 'YYYYMMDDTHHmmSS/YYYYMMDDTHHmmSS' // inicio y fin en zona horaria del usuario
  // let DETAILS = ''
  const LOCATION =
    'La Goblinera - C. del Escultor Benlliure, 3, 50002 Zaragoza, AR, es'.replace(
      / /g,
      '+'
    )
  const SPROP_WEBSITE = 'sprop=website:https://www.goblinera.net'
  const SPROP_NAME = 'sprop=name:La Goblinera'.replace(/ /g, '+')
  const ADD = process.env.REACT_APP_GOBLIN_USER

  return (
    <Fragment>
      <a target='\_blank' href={BASEURL}>
        {' '}
        <img
          border='0'
          src='https://www.google.com/calendar/images/ext/gc_button1_es.gif'
        />
      </a>
    </Fragment>
  )
}

export default SendToCalendar
